apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: rtscs-services
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
  - name: http
    port: 80
    targetPort: 3003
    protocol: TCP
